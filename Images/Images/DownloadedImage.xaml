<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Images;assembly=Images"
    x:Class="Images.DownloadedImage">

    
<AbsoluteLayout>
   
        <StackLayout Orientation="Horizontal"
            AbsoluteLayout.LayoutBounds="0,0,1,50"
            AbsoluteLayout.LayoutFlags="WidthProportional">
<!--            proportional... %, otherwise the others are pixcels-->
                
           
                <Button Text="←"
                        BackgroundColor="White"
                        HorizontalOptions="StartAndExpand"/>

                <Button Text="→"
                        BackgroundColor="White"
                        HorizontalOptions="EndAndExpand"/>

                
        </StackLayout>

            <Image 
                   x:Source="{local:ImageResource Images.beach.jpg}" 
                   x:Name="embeddedImage"
                   AbsoluteLayout.LayoutBounds="0,1,1,1"
                   AbsoluteLayout.LayoutFlags="SizeProportional" />
  
        
</AbsoluteLayout>
    
</ContentPage>
